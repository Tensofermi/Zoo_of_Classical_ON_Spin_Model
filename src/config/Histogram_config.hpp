# pragma once

void Histogram::initDistr()
{
    saveOrNot = true;

    // addDistr(para.his_c1, "log",     "c1_" + toStr(para.L),       0,    100,    1000);
    // addDistr(para.his_ns, "log",     "ns_" + toStr(para.L),       0,    100,    1000);
}